# Mengaktifkan modul rewrite
RewriteEngine On

# Mengarahkan semua request yang BUKAN file atau direktori yang ada ke index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
